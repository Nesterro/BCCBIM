<Window x:Class="BCCPlugIn.SumParametersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIMBCC — Сумма параметров" Height="520" Width="720"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="#E0E0E0"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#252526" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="BIMBCC — Расчет суммы параметров выделения" FontSize="15" FontWeight="Bold" Foreground="#FFFFFF"/>
                <TextBlock x:Name="SelectedCountTextBlock" Text="Выделено элементов: 0" FontSize="11" Foreground="#AAAAAA" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- Coefficient Input Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Множитель / Коэффициент:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <TextBox x:Name="CoeffTextBox" Grid.Column="1" Text="1.0" Padding="6,4" TextAlignment="Center" Background="#2D2D30" Foreground="#FFFFFF" BorderBrush="#434346" TextChanged="CoeffTextBox_TextChanged"/>
            <TextBlock Grid.Column="2" Text="(Например: 1.15 для запаса, 0.001 для мм -> м)" VerticalAlignment="Center" Foreground="#888888" Margin="10,0,0,0"/>
        </Grid>

        <DataGrid x:Name="StatsDataGrid" Grid.Row="2" AutoGenerateColumns="False"
                  Background="#2D2D30" RowBackground="#252526" AlternatingRowBackground="#2D2D30"
                  Foreground="#FFFFFF" BorderBrush="#3F3F46" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#3F3F46" HeadersVisibility="Column" RowHeight="36"
                  CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333337"/>
                    <Setter Property="Foreground" Value="#007ACC"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Height" Value="38"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="#3F3F46"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя параметра" Binding="{Binding ParameterName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding FormattedSum}" Width="130" IsReadOnly="True"/>
                <DataGridTextColumn Header="Сумма с коэфф." Binding="{Binding FormattedSumWithCoeff}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding UnitName}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Count}" Width="70" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Закрыть" Width="90" Padding="10,6" Background="#333337" Foreground="#FFFFFF" BorderBrush="#555555" BorderThickness="1" Click="CloseButton_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
