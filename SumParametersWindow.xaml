<Window x:Class="BCCPlugIn.SumParametersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIMBCC — Сумма параметров" Height="500" Width="680"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="#E0E0E0"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#252526" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="BIMBCC — Статистика параметров выделения" FontSize="15" FontWeight="Bold" Foreground="#FFFFFF"/>
                <TextBlock x:Name="SelectedCountTextBlock" Text="Выделено элементов: 0" FontSize="11" Foreground="#AAAAAA" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="StatsDataGrid" Grid.Row="1" AutoGenerateColumns="False"
                  Background="#2D2D30" RowBackground="#252526" AlternatingRowBackground="#2D2D30"
                  Foreground="#FFFFFF" BorderBrush="#3F3F46" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#3F3F46" HeadersVisibility="Column"
                  CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333337"/>
                    <Setter Property="Foreground" Value="#007ACC"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="#3F3F46"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя параметра" Binding="{Binding ParameterName}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding FormattedSum}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Среднее" Binding="{Binding FormattedAvg}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Мин" Binding="{Binding FormattedMin}" Width="90" IsReadOnly="True"/>
                <DataGridTextColumn Header="Макс" Binding="{Binding FormattedMax}" Width="90" IsReadOnly="True"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Count}" Width="60" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Закрыть" Width="90" Padding="10,6" Background="#333337" Foreground="#FFFFFF" BorderBrush="#555555" BorderThickness="1" Click="CloseButton_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
